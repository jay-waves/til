* `==二级标题级别`
* `_斜体_`
* `*粗体*·
* `` `行内代码` ``
* `<标签>`
* `@引用标签或bib`
* `-无序列表`
* `+有序列表`
* `#图片` 
* `#表格` 
* `$公式$` `$ 行间公式 $`

typst 中使用 `#` 前缀来标记命令（表达式）。

```typst
#rect(
  width: 2cm,
  height: 1cm,
  stroke: red,
)
```