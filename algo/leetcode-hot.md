## 二分查找

* 精确查找
* 上下界查找
	* 0034（排序数组中查找重复元素的第一个和最后一个位置）：二分法找上下界即可

## 排序

快速排序：三向快速排序方法，
堆排序，
归并排序，
链表的归并排序，
计数排序（桶排序）


* 0581（最短无序子数组）
* 0075（颜色分类）：简易桶排序
* 0406（Queue Reconstruction by Height）：纯纯脑筋急转弯，第一步排序，第二部类似贪心

#### TopK 问题

* 0347（统计前 K 个高频元素）：用优先队列记录 `<freq, nums>`

#### 滑动窗口

* 0438（Find All Anagrams in s String）：滑动窗口 + 桶计数

## 链表

* 0021（合并两个有序链表，容易）
* 0160（相交链表，容易）
* 0141（环形链表，容易）
* 0142（环形链表2）：快慢指针方法
* 0002（两数和）：链表遍历
* 0023(合并 k 个升序链表）：归并排序非常适合链表。

## 栈

* 0020（有效的括号）
* 0032（最长有效括号）：也可以用栈，栈中记录 `{idx, '('}`，将有效括号子串映射为 bimap 中的连续 1
* 

## 队列

#### 单调（双端）队列

* 滑动窗口

## 哈希表

* 0004（LRU）：双向链表 + 哈希表

## 位图

* 0034（统计 bit1 数量，容易）：不断右移判断奇偶即可
* 

## 平衡树

#### 平衡树的遍历

深度优先搜索：前序、中序、后序

层序遍历：引入队列记录未处理节点

* 0101（对称二叉树，容易）
* 0297（二叉树序列化）
* 

## 异型树

* 0208（Trie 树）

## 图

## 矩阵

* 0048（原地旋转矩阵）
* 岛屿

## 动态规划

* 0070（爬楼梯）
* 0062（路径和，容易）

#### 01背包

受限资源下，选择一组物品（有权重）使目标最优（如价值最大、凑出精确值）。每个物品仅选择一次。

- 0053（最大数组和）：01背包问题。`dp[i] = max(nums[i], dp[i-1] + nums[i])` `dp[i]` 指以当前位置结尾的最大子数组和
- 0064（矩阵中限定方向的最小路径和）：`dp[i][j]` 可能来自于 `dp[i-1][j]` 或 `dp[i][j-1]`
- 0070（爬楼梯）
- 0221（最大正方形）：？？？

#### 完全背包

受限资源下，选择一组物品（有权重）使目标最优。每个物品可选择无限次。

- 0887（扔鸡蛋）

## 贪心法

#### 前缀和

## 回溯法

#### 组合问题

从 1...n 中无序选取 k 

* 0015（三数之和）：排序 + 中点法
* 

#### 子集问题

给定数组，返回某些条件下的子集

* 0022（括号生成）

#### 排列问题

全排列 n 个数

* 0046（全排列）：回溯法，记录：历史路径、是否被占用


## 字符串

* 0003（无重复字符的最长子串）：双指针方法
* 0005（最长回文子串）：中点法

#### 距离

汉明距离

编辑距离

最长公共子串

## 脑筋急转弯（数学）

* 0754（Reach A Number）

