记录一些 C/C++ 位运算的奇技淫巧

### 向上取整

`(x + y - 1) / y` 是 `x / y` 的向上取整。

### 按位取反

```c
~s
```

### 对齐到 2 的幂边界

```c
// align = 2^k
(x + align - 1) & ~(align - 1)
```

### 统计比特 1 的个数

```c
while (x) {
	x &= x - 1;
	cnt++;
}
```