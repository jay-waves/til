
参数：
* *核心线程数*：任务队列未到达容量时，稳定的线程数，不会轻易销毁。
* *最大线程数*：任务队列达到容量上限时，扩容线程数
* *存活时间*：扩容的线程的存活时间
* *线程取消与退出令牌*：
* *任务*：Callable 接口。可能有状态机（提交、执行、错误、完成）
* *任务队列*：用于存储等待执行的任务

异常处理：
* 任务遇到错误时，如何退出？
* 

性能参数：
* 任务调度：降低锁竞争，兼顾任务公平性
* 可观测性：日志、任务分配情况
* 弹性：线程数动态调整

### v1

这个 batch 实现地并不好，有 CV 抖动。。。

存在全局锁瓶颈。比如有特别多小任务。

### v2

### v3

加入弹性：自动扩展工作线程数
